{{ define "header" }}
  <nav class="header-nav">
    <div>
      <a class="logo" href="{{ .App.FrontEndURLNoProto }}">
        <img
          src="{{ .App.PublicURLNoProto }}/logo.svg"
          alt="{{ .App.Config.ApplicationName }} logo"
        />{{ .App.Config.ApplicationName }}
      </a>
    </div>
    <div style="text-align: right">
      {{ if .User }}
        {{ if .User.IsAdmin }}
          <a href="{{ .App.FrontEndURLNoProto }}/web/admin">{{ call .T "Admin" }}</a>
        {{ end }}
        <a href="{{ .App.FrontEndURLNoProto }}/web/user"
          >{{ call .T "%s's Account" .User.Username }}</a
        >
        <form
          style="display: inline"
          action="{{ .App.FrontEndURLNoProto }}/web/logout"
          method="post"
        >
          <input type="submit" value="{{ call .T "Log out" }}" />
        </form>
      {{ else }}
        <a href="{{ .App.FrontEndURLNoProto }}/web/registration">{{ call .T "Registration" }}</a>
      {{ end }}
    </div>
  </nav>

  {{ if .ErrorMessage }}
    <p class="error-message">{{ .ErrorMessage }}</p>
  {{ end }}
  {{ if .SuccessMessage }}
    <p class="success-message">{{ .SuccessMessage }}</p>
  {{ end }}
  {{ if .WarningMessage }}
    <p class="warning-message">{{ .WarningMessage }}</p>
  {{ end }}
{{ end }}
